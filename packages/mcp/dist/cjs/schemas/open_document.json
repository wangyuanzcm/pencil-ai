{
  "annotations": {
    "title": "Open Document",
    "readOnlyHint": false,
    "destructiveHint": true,
    "idempotentHint": false,
    "openWorldHint": true
  },
  "description": "Use this tool in the following two scenarios ONLY:\n- if there is no active editor opened currently, pass \"new\" to open new document or\n- if user requests to open a specific .pen file, pass the absolute file path to open it.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "filePathOrTemplate": {
        "description": "File path to an existing .pen file, OR 'new' for a new document. No other template names are valid.",
        "type": "string"
      }
    },
    "required": [
      "filePathOrTemplate"
    ]
  },
  "name": "open_document"
}
